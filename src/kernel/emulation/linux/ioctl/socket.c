#include "socket.h"
#include <sys/errno.h>

int handle_socket(int fd, unsigned int cmd, void* arg, int* retval)
{
	// BEWARE: This function is not yet wired into the logic and the build
	return IOCTL_PASS;
}


